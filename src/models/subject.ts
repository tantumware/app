export class Subject {

    constructor(public nome: string, public codigo: string, public fase: number, public professor: string, public aulas: number, public obrigatoria: boolean, public horarios: string[], public requisitos: string[]) { }

}